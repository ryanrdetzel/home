My home directory files.


Notes for myself.
-----------------

sudo apt-get update && sudo apt-get install zsh
chsh -s /bin/zsh

Newest Vim
sudo apt-get install python-software-properties
sudo apt-add-repository ppa:blueyed/ppa
sudo apt-get update
sudo apt-get upgrade


Install powerline from source
https://github.com/Lokaltog/powerline.git

* Change .vimrc line to point to the bundle path on that system
* Copy the config.json file from that directory into ~/.config/powerline/
* Change the coloreschema for vim to solarized


pip install pyflakes



